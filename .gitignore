node_modules/
dist/
.env
.env.local
.env.bak*
*.log
.DS_Store

# Generated runtime artifacts
data/cache/
data/out/
data/discovered/
data/campaign/

# OAuth secrets (Gmail) — never commit
secrets/

# Personal ICP config (regenerated by `npm run gen:demo`; commit the .example)
config/icp.json

# MCP tool servers (cloned repos, local builds)
tools/

# MCP config (contains API keys)
.mcp.json
.tmp/
